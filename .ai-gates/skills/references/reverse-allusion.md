# 逆向总结典故（归档压缩提名）

> evolution-02 归档「逆向总结典故」。定位=**归档总结提名**，显式 **≠逆链**（窗内校验见既有 [reverse-chain.md](./reverse-chain.md)，本页禁止改该文件）。
> **加载本文件 ≠ 每个归档必跑。**

## 用途分级（硬）

| 级 | 条件 | 动作 |
| --- | --- | --- |
| **跳过** | 窗内无改前选型三格（【本步方案】【为什么】【不选的】缺任一项或无表） | 整段不跑；**禁止**补跑逆链；**禁止**补写 why |
| **不跑** | 空闲枢纽迁签收 | 不跑（只 migrate） |
| **不跑** | Express 默认 | 不跑（Express 通常无三格=跳过；禁止为 Express 补写三格再跑） |
| **跑** | `completed` 或失败/止损封存 **且** 同条 `migrate-pipeline-window.ps1` **且** 有改前三格 | 从结案事实回指三格原文，压成典故卡草稿 |

## 何时

仅上表「跑」。点名「逆向总结典故」=加载本页并按分级执行（无三格仍跳过）。点名「逆向总结」**≠**启用逆链。

## 输入

已冻选型三格原文。有逆链产物更好 → 只可放入「本窗验证」栏当引用，不得据此新编【为什么】。

## Procedure（压缩提名 · 非倒走补 why）

1. 读结案事实：哪些 A# 过了 / 冻结表没破 / 止损因哪条炸（失败封存）。
2. 回指三格**原文**（本步方案 / 为什么 / 不选的），只许引用，不许新编【为什么】。
3. 压成一条约五字段典故卡草稿（对齐 [design-patterns.md](./design-patterns.md) 五格，**不当新表**）。

## 产出

追加到**该结案窗** `证据/_lesson-pending.md`；若已有 §F 成功经验 YAML → **追加**本节，禁止覆盖。pending 不生效。

## 逆向总结典故卡

来源：逆向总结

| 典故词候选 | 触发症状 | 适用/结构 | 本窗验证（A#/冻结表原文） | 禁用边界 |
| --- | --- | --- | --- | --- |
| … | … | … | 引用原文 | … |

## 邻机制

| 机制 | 文件 | 关系 |
| --- | --- | --- |
| 逆链 | [reverse-chain.md](./reverse-chain.md) | 窗内校验；本页=归档压缩；点名互不启用 |
| 分歧 | [divergence-annotation.md](./divergence-annotation.md) | ≠ |
| 碰撞 | [collision-review.md](./collision-review.md) | ≠；pending 层可对齐「不生效」，不改其回灌门槛；碰撞侧划界「本文件不建归档总结页」 |
| 设计模式词条 | [design-patterns.md](./design-patterns.md) | 五字段对齐；本页不写入词条表 |
| 共享语言典故 | [shared-language.md](./shared-language.md) | 本页不写入词条表 |
| 晋升 | lessons 机制 B / 01-B | pending → 准进主表 → 01-B → 再准入典；本页不实施 |

## 晋升（只声明，本页不实施）

pending → 用户准进 lessons 主表 → 既有 01-B 候选 → 再准才入典。流程类将来进 §典故，代码结构进 design-patterns。**禁止**热度满自动入典。**禁止**写 gh 已接线。

## 借口表

| 借口 | 驳回 |
| --- | --- |
| 「每个归档都该总结」 | 无三格必须跳过 |
| 「没 why 就跑一次逆链补上」 | A3 禁止（无三格不得补跑逆链、不得新编 why） |
| 「热度够了直接入典」 | 须 01-B + 用户准 |
| 「点名逆向总结就是逆链」 | 正交；点名「逆向总结」≠启用逆链 |

## 禁止

- 直接改 shared-language / design-patterns 词条表；第三张 AI 最优设计表；第四张逻辑节点表；`docs/adr/`。
- Express 默认跑；每个归档必跑；改 reverse-chain Procedure；逆链内部热度计。
- 自报覆盖率；自报「已入典」；写 gh 已接线。
